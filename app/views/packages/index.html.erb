<div id="app">
  <div class="wrapper row3">
    <main class="hoc container clear btmpad-none">
      <div class="content">
        <% if flash[:error]  %>
          <div class="alert alert-warning"> <%= flash[:error] %></div> 
        <% end %>
        <% if flash[:notice]  %>
          <div class="alert alert-success"> <%= flash[:notice] %></div> 
        <% end %>

        <h2 class="receivername">物品明细</h2>
        <div id="comments" class="scrollable">
          <table>
            <thead>
              <tr>
                <th>名称</th>
                <th>数量</th>
                <th>产地</th>
                <th>修改</th>
              </tr>
            </thead>
            <tbody>
              <tr v-for="item in packageItems">
                <th>{{item.name}}</th>
                <th>{{item.quantity}}</th>
                <th>{{item.price}}</th>
                <th><a class="btn smlbtn inverse" v-bind:click="removePackageItem(item)">删除</a></th>
              </tr>
            </tbody>
          </table>
          <%= form_tag do %>
              <div class="one_third first">
                <%= label_tag(:name, '名称') %>
                <%= text_field_tag(:name, '', :required => true, 'v-model': 'packageItem.name') %>
              </div>
              <div class="one_third">
                <%= label_tag(:quantity, '数量*') %>
                <%= number_field_tag(:name, '', :required => true, :min => 0, :step => 1, 'v-model': 'packageItem.quantity') %>
              </div>
              <div class="one_third">
                <%= label_tag(:price, '单价') %>
                <%= number_field_tag(:price, '', :min => 0, 'v-model': 'packageItem.price') %>
              </div>
              <div class="block clear">
                <%= check_box_tag '轻奢侈品' %>
              </div>
              <div>
                <a class="btn smlbt" href="" v-on:click="addPackageItem()">确定</a>
              </div>
          <% end %>
        </div>
      </div>
    </main>
  </div>
  <div class="wrapper row3">
    <main class="hoc container clear btmpad-none">
      <div class="content">
        <div id="comments"> 
          <h2 class="receivername">选择收件人地址</h2>
          <p class="divider">收件人是否在列表中？如果是，请点击选此收件人。若否，请<%= link_to '新建收件人地址', new_address_path(:returnUrl => packages_path) %></p>
          <p>列表太长？请在下框中搜索（比如，姓名，电话，身份证等）</p>
          <div class="one_third first">
            <input type="text" placeholder="姓名，电话，身份证" v-model="query"> 
          </div>
          <address-label v-for="address in addresses" v-bind:selected="selectedAddress === address" v-bind:address="address" v-on:delete="removeAddress(address)" v-on:selected="selectAddress(address)"></address-label>
        </div>
      </div>
    </main>
    <div class="chose">
      <a class="btn" href="" v-click="submitPackage()"   
    </div>
  </div>
</div>
